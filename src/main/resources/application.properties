# Nome da aplicação
spring.application.name=EcoSafe

# Configurações do datasource (PostgreSQL)
spring.datasource.url=jdbc:postgresql://database:5432/eco_safe
spring.datasource.username=postgres
spring.datasource.password=ecosafe123!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# JWT
jwt.secret=ecosafe-secret-key-2024-muito-seguro

# Porta do servidor
server.port=8080

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html